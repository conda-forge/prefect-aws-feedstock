{% set name = "prefect-aws" %}
{% set version = "0.7.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/prefect_aws-{{ version }}.tar.gz
  sha256: 549110fe69799ed0971bc31f14c3659947c944d88f8752301d6c4c2ebc1e336e

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - aiobotocore >=2.23.2
    - boto3 >=1.24.53
    - botocore >=1.27.53
    - mypy-boto3-s3 >=1.24.94
    - mypy-boto3-secretsmanager >=1.26.49
    - prefect >=3.6.22
    - pyparsing >=3.1.1
    - rich >=10.0.0
    - tenacity >=8.0.0
    - typer >=0.9.0

test:
  requires:
    - python {{ python_min }}
  imports:
    - prefect_aws
# Disable `pip check` until `prefect` adds `pip check` support
#  commands:
#    - pip check
#  requires:
#    - pip

about:
  home: https://github.com/PrefectHQ/prefect-aws
  summary: Prefect collection of tasks and subflows to integrate with AWS
  license: Apache-2.0
  license_file: LICENSE
  license_family: Apache
  doc_url: https://prefecthq.github.io/prefect-aws/
  dev_url: https://github.com/PrefectHQ/prefect-aws


extra:
  recipe-maintainers:
    - mikeoconnor0308
    - peterbygrave
    - skrsna
    - ahuang11
    - desertaxle
